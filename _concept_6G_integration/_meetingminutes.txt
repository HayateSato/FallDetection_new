2026.04.13.

I got few more infors

For dashboard, it should have two types of user role 
1	admin 	No patient info (encrpted)  but health status of the services only,  ml model
2	caregiver	access to patients info (only the patients in the group/nurse station/floor)

For Data fetcher, fetcher for currnet system and inference API should be separeted

For their machine spec, they are using 		
	12cores 32GB CPU
    current system is running in the same namespace of helm Charts
    they want to just run helm chart and get everything running (I suppose we can create a separate helm chart that runs all the additional components and point its entry/end points to exisitng system)

Look for the architecture overview (_concept_6G_integration\6G_architecture_overview.png)


For the events manager it should be managed by MQTT so that bidrictational communication can happen between mobile and inference component


Questions								
										
Sensor + Data Storage										
 - 	What is the accelerometer sample rate and which sensor is it? (We assume 25Hz Bosch)									
 - 	Where is raw sensor data stored (InfluxDB? Where is it hosted?)									same namesapaces
	  - Bucket name? Measurement name?									Isa
	  - What tag identifies each patient/device? (We use `macAddress`)									macAddress + Patient ID
	  - What field names for ACC axes? (We use `bosch_acc_x/y/z`)									Isa
										
Dashboard + Monitoring System										
 - 	How can the dashboard receive real-time event (Browser: Server-Sent Evenrts.  Bbackend: Redis)? 									MQTT
 - 	Is there a developer who has access to modify the dashboard?									Andreea?
