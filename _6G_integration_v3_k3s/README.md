# Fall Detection — FOCUS Caregiver Layer (k3s)

Self-contained repository for the FOCUS-hosted caregiver services.
Targets FOCUS's existing k3s cluster with Traefik.

Two services deployed: **mosquitto** (MQTT broker) + **fall-dashboard** (SSE API).
No Postgres. No mock-app (replaced by the real Isa mobile app in production).

---

## Structure

```
_6G_integration_v3_k3s/
│
├── README.md                              this file
│
├── fall_dashboard/                        Python service — MQTT subscriber + SSE API
│   ├── Dockerfile                         builds the fall-dashboard image
│   ├── requirements.txt
│   ├── main.py                            entry point + MQTT callback wiring
│   ├── web.py                             FastAPI routes (/api/patients, /api/falls, /api/stream SSE)
│   ├── db.py                              InfluxDB queries (fall history + fall counts)
│   ├── mqtt_listener.py                   FallEventBroker — paho MQTT -> asyncio SSE bridge
│   ├── patient_store.py                   SQLite patient store (patient_id, MAC address)
│   └── dashboard/                         local-test browser UI (not used in K3s production)
│       ├── index.html
│       ├── app.js
│       ├── style.css
│       ├── patient.html
│       └── patient.js
│
├── helm/                                  Helm chart for K3s deployment
│   ├── Chart.yaml
│   ├── values.yaml                        local two-laptop testing values (NodePorts, local image)
│   ├── values_production.yaml             production values template — fill in all CHANGE_ME fields
│   ├── build.ps1 / build.sh               build + push fall-dashboard image to registry (MCS runs this)
│   ├── install.ps1 / install.sh           helm upgrade --install wrapper
│   ├── test.ps1 / test.sh                 7-probe smoke test (run after install)
│   ├── teardown.ps1 / teardown.sh         helm uninstall + delete namespace
│   ├── README.md                          helm-level deployment notes
│   ├── extras/
│   │   └── traefik-mqtts-entrypoint.yaml  DEPRECATED — do not apply
│   └── templates/                         K8s manifests (generated by Helm)
│       ├── _helpers.tpl
│       ├── mosquitto-configmap.yaml       mosquitto.conf injected as ConfigMap
│       ├── mosquitto-deployment.yaml      mosquitto pod (eclipse-mosquitto:2)
│       ├── mosquitto-service.yaml         ClusterIP :1883 (TCP) + :9001 (WS)
│       ├── mosquitto-ingressroute.yaml    Traefik IngressRoute — WSS on port 443
│       ├── mosquitto-ingressroutetcp.yaml DEPRECATED — do not apply
│       ├── mosquitto-pvc.yaml             500Mi PVC for broker persistence
│       ├── fall-dashboard-deployment.yaml fall-dashboard pod
│       ├── fall-dashboard-service.yaml    ClusterIP :8002
│       ├── fall-dashboard-ingressroute.yaml Traefik IngressRoute — HTTPS on port 443
│       ├── fall-dashboard-configmap.yaml  environment config
│       ├── fall-dashboard-secret.yaml     InfluxDB token + MQTT credentials
│       └── fall-dashboard-pvc.yaml        500Mi PVC for SQLite patient store
```

---

## Quick start

### 1. One-time cluster setup (FOCUS DevOps, done once)

```powershell
# Create namespace + registry pull secret
kubectl create namespace fall-dashboard
kubectl create secret docker-registry mcs-labs `
    --docker-server=registry-smarko-health.de `
    --docker-username=<user> `
    --docker-password=<pass> `
    --namespace fall-dashboard
```

### 2. Fill in values_production.yaml

Open [helm/values_production.yaml](helm/values_production.yaml) and replace every `CHANGE_ME`.

### 3. Build image and deploy

Run from this directory as working directory:

```bash
bash helm/build.sh      # build + push fall-dashboard image
bash helm/install.sh    # helm upgrade --install
bash helm/test.sh       # smoke test (expected: 7/7 PASS)
```

---

## Connection addresses after deployment

| Client | Protocol | Address |
|---|---|---|
| Mobile app (Isa) | MQTT over WSS | `wss://mqtt.<FOCUS-domain>` (port 443) |
| Flutter dashboard | HTTPS | `https://fall.<FOCUS-domain>/api/stream` |
| fall-dashboard -> mosquitto | MQTT (internal) | `mosquitto:1883` |
| fall-dashboard -> InfluxDB | HTTPS | configured via `INFLUXDB_URL` in values |

## MQTT topics

| Topic | Publisher | When |
|---|---|---|
| `fall/possible/<patient_id>` | Mobile app | Immediately on fall detection |
| `fall/alert/<patient_id>` | Mobile app | After patient confirmation or 10s timeout |

---

## Related

- [`_6G_integration_v3_docker_mcs/`](../_6G_integration_v3_docker_mcs/) — MCS inference layer (Docker Compose, deploy to Hetzner)
- [`_6G_integration_v3_docker_focus/`](../_6G_integration_v3_docker_focus/) — Docker Compose version of this caregiver layer (two-laptop local testing)
- [`FOCUS_devs_handover/production_config_checklist.md`](../FOCUS_devs_handover/production_config_checklist.md) — full config table for both layers
