Update the todo document based on below 

1. Confirm VPN (WireGuard) is NOT required --> i can confirm. maybe for testing we might need it but we will not use it in real deploement

2. Infrastructure -- MCS Side (Inference & Post-training Layer)
hosting env --> we use hetzner in our company so most likely this 
a public domain  --> we have own company domain so we can re-use this too
Deployment of inference server, postgres, mlflow, prometeheus, ml-dashbaord and sever health-dashbaord will be done through my colleague Mohamme, but i want to prepare docker or something after testing locally again so that he can just deploy it easiyl
we will not use kubernetes on our end if technologically that is still compaitble 

3. Code Changes -- Inference Server (MCS)
remove fall_history table from MCS Postgres --> implement this for me 
to test the whole pipleine, update the mock mobile app script that is running in our namespace so that it injects the timestamps (if the new field name is required, create whatever you think matching ) and the fall dashabord can fetch the results.



4. Code Changes -- Caregiver Dashboard (FOCUS side)
their dashbaord is built in flutter. based on the features implemented in fall_dashboard (which is python), we need to provide them instrcution documents, what compoent and features are required, how and from where they should get the fall_history




Open Questions

3. What InfluxDB fields/measurement names will FOCUS use for fall timestamps? Needed to align caregiver dashboard query --> we can decide this. 
4.	Does the caregiver dashboard need help_requested and fall_confirmed fields in InfluxDB, or is the timestamp enough? --> yes, we need those fields in influxDB
5.	Who updates the caregiver webapp server (fetch from InfluxDB) -- MCS or FOCUS DevOps? --> We will push this to FOCUS , so we need to prepare some instruction documents later

 Prepare Docker Compose deployment package (NOT Kubernetes -- MCS will not use K8s on their side)


 ------ above is implemented already ---- below is not yet ---------

checking fall_dashbaord 
- when it shows the detected fall (when marking patient ID with affected patient in red), does it do via MQTT or the animation happens based on the rsult of data fetching from influx?
- change I want: currently caregiver dose not get instant potential alert message, but only when the fall is confirmed. I was requested that they want to see a messages "possible fall, wait for confirmation" for the affected patient (without populating the dashboard display too much or clamemd)