# Retraining pipeline dependencies
# pip install -r retrain/requirements.txt

# MLflow — experiment tracking, model registry, artifact store.
# Pin must match infrastructure/mlflow/Dockerfile and inference_server/requirements.txt
# to avoid REST API drift between client and server.
mlflow==3.11.1

# ML
xgboost>=1.7
scikit-learn>=1.3
numpy
pandas
joblib

# DB access (shared with other components)
sqlalchemy>=2.0
psycopg2-binary

# MinIO / S3 — required for MLflow artifact store when MLFLOW_ARTIFACT_ROOT=s3://...
boto3

# Configuration
python-dotenv

# InfluxDB (only needed for --influxdb mode in seed_test_data.py)
influxdb-client
