apiVersion: v1
kind: Service
metadata:
  name: ml-dashboard
  namespace: {{ .Values.namespaces.ours }}
spec:
  selector:
    app: ml-dashboard
  ports:
    - port: {{ .Values.mlDashboard.port }}
      targetPort: {{ .Values.mlDashboard.port }}
