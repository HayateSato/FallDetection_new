apiVersion: v1
kind: Service
metadata:
  name: fall-dashboard
  namespace: {{ .Values.namespaces.ours }}
spec:
  selector:
    app: fall-dashboard
  ports:
    - port: {{ .Values.fallDashboard.port }}
      targetPort: {{ .Values.fallDashboard.port }}
