# Fall Dashboard dependencies
# pip install -r fall_dashboard/requirements.txt

# Web server
fastapi
uvicorn[standard]

# DB layer (shared schema with inference_server)
sqlalchemy>=2.0
alembic
psycopg2-binary

# Sync HTTP client (used to call the inference server)
requests

# MQTT client (subscribes to fall events from MQTT broker)
paho-mqtt>=2.0

# InfluxDB v2 client (poller queries the time-series store)
influxdb-client

# Numerical processing for raw sensor arrays
numpy
pandas

# Configuration
python-dotenv
