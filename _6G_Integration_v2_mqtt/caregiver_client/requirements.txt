# Caregiver client dependencies
# pip install -r caregiver_client/requirements.txt

# Web server
fastapi
uvicorn[standard]

# DB layer
sqlalchemy>=2.0
# Optional Postgres driver — uncomment if DATABASE_URL points at Postgres
# psycopg2-binary

# Sync HTTP client (used to call the inference server)
requests

# Async Redis client (subscribes to fall_events)
redis>=5.0

# InfluxDB v2 client (poller queries the time-series store)
influxdb-client

# Numerical processing for raw sensor arrays
numpy
pandas

# Configuration
python-dotenv
