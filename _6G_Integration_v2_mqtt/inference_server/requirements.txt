# 6G Integration — inference server dependencies
# pip install -r inference_server/requirements.txt

# ML pipeline
numpy
pandas
scipy
scikit-learn
xgboost
joblib

# API server
fastapi
uvicorn[standard]

# Async HTTP client (for pushing observations to FHIR server)
httpx

# MLOps — Prometheus metrics (optional — server still works without these)
prometheus-client
prometheus-fastapi-instrumentator

# Database — inference log (BackgroundTask write to SQLite/Postgres)
sqlalchemy>=2.0
# MLflow — used by /model/switch to load models from registry (Step 11c)
mlflow>=2.10
psycopg2-binary
alembic

# MinIO / S3 — required when /model/switch loads from MLflow registry backed by MinIO
boto3

# Configuration
python-dotenv
